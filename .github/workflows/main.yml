name: Generate Lighthouse Report

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Get Report
        run: |
          npm install -g lighthouse
          npm set prefix ~/.npm
          printf "PATH=\"$HOME/.npm/bin:$PATH\"" >> ~/.bashrc
          printf "PATH=\"./node_modules/.bin:$PATH\"" >> ~/.bashrc
          lighthouse https://raybo.org/githubchallenge/
